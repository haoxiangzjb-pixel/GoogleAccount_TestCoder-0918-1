Nothing should be ignored in this case, as the added files are source code files (.cpp and .h) which are part of the project's source code and should be tracked in version control. There are no build artifacts, dependencies, or temporary files in the changes that need to be ignored.