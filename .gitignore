Nothing should be ignored based on the provided changes. The added file `get_current_user_info.ps1` is a PowerShell script file, which is source code and should be tracked in version control. No build artifacts, dependencies, or temporary files are present in the changes.