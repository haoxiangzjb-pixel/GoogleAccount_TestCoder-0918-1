(get_current_user_info.ps1 is a PowerShell script file, which is a source code file, so no files in the changes match the ignore patterns for build artifacts, dependencies, temp files, etc.)

(empty - no output)