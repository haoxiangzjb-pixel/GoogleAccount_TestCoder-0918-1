```
# Kubernetes/Docker artifacts
*.yaml
*.yml

# If this is a Kubernetes deployment file, we might want to keep it
# But if there are generated deployment files, we can ignore them
*deployment-*.yaml
```