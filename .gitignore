```
# Kubernetes deployment files
*.yaml
*.yml

# If this is a Kubernetes project, you might want to ignore other common files
# but based on the changes shown, only ignoring the specific file type
```