Nothing should be ignored based on the provided file changes. The added files (`number_processor.fsx` and `o0zUFQLKca.txt`) are source code and text files that should be tracked in version control.
packages-microsoft-prod.deb
