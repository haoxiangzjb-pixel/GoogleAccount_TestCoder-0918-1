[application]
name = "MyApp"
version = "1.0.0"
debug = true

[database]
host = "localhost"
port = 5432
username = "user"
password = "secret"

[server]
host = "0.0.0.0"
port = 8080

[logging]
level = "info"
file = "app.log"