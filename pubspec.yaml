name: network_data_fetcher
description: A simple command-line application to fetch data from the network.
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  http: ^1.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0