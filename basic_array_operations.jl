# Basic Array Operations in Julia

# Define a sample array
arr = [10, 20, 30, 40, 50]

println("Original Array: ", arr)

# Calculate the sum of the array
sum_arr = sum(arr)
println("Sum of Array: ", sum_arr)

# Calculate the mean of the array
mean_arr = mean(arr)
println("Mean of Array: ", mean_arr)
