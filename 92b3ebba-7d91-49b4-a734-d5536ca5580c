proc printMultiplicationTable() =
  for i in 1..9:
    for j in 1..i:
      stdout.write(j, "x", i, "=", i*j)
      if j < i:
        stdout.write("\t")
    stdout.write("\n")

printMultiplicationTable()