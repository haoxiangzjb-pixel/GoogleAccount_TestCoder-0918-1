// Функция для сложения двух чисел
function sum(a, b) {
  return a + b;
}

// Пример использования
console.log(sum(2, 3)); // Выведет 5
console.log(sum(-1, 5)); // Выведет 4

// Экспорт функции для возможности использования в других файлах (опционально)
// module.exports = sum; // Для Node.js
// export { sum }; // Для ES6 модулей