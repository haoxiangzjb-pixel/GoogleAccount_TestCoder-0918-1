# Load CSV file and print summary statistics
# Read the CSV file (assuming it's named 'data.csv' in the current directory)
data <- read.csv("data.csv")

# Print summary statistics
print(summary(data))