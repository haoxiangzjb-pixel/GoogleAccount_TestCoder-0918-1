# Project Name

A brief description of the project goes here. This README outlines the installation and usage instructions.

## Installation

To install the necessary dependencies for this project, run the following command:

```bash
npm install
```

Or, if using Yarn:

```bash
yarn install
```

## Usage

To run the project locally, use the following command:

```bash
npm run dev
```

Or, if using Yarn:

```bash
yarn dev
```

The application should now be running on `http://localhost:3000`.