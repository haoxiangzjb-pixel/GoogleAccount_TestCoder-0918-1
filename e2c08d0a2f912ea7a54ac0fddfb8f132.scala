// Функция для суммирования списка чисел
def sumList(numbers: List[Int]): Int = {
  numbers.sum
}

// Пример использования (закомментирован для предотвращения выполнения при компиляции)
// val myList = List(1, 2, 3, 4, 5)
// println(sumList(myList)) // Выведет 15